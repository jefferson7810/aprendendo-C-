/*#include <iostream>
#include <climits>

// Função para verificar se um número cabe em 16 bits (short int)
bool isShort(long long number) {
    return (number >= SHRT_MIN && number <= SHRT_MAX);
}

// Função para verificar se um número cabe em 32 bits (int)
bool isInt(long long number) {
    return (number >= INT_MIN && number <= INT_MAX);
}

int main() {
    long long num;

    std::cout << "Digite um número para verificar se cabe em 16 bits: ";
    std::cin >> num;
    std::cout << "O número " << num << " cabe em 16 bits? " << std::boolalpha << isShort(num) << std::endl;

    std::cout << "Digite um número para verificar se cabe em 32 bits: ";
    std::cin >> num;
    std::cout << "O número " << num << " cabe em 32 bits? " << std::boolalpha << isInt(num) << std::endl;

    return 0;
}*/
